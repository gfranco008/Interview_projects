# Agent Instructions

This repo contains multiple projects. Scope work to a single project folder.

When the user specifies a folder:
- Treat it as the working project root.
- Read `<folder>/AGENTS.md` first if it exists.
- Keep searches, edits, and tests inside that folder unless the user asks otherwise.
- If cross-project changes are needed, explain why and ask.

Projects:
- `qvest`: Reading Momentum POC. Key paths: `qvest/README.md`, `qvest/backend`, `qvest/frontend`, `qvest/data`, `qvest/assets`, `qvest/presentation.md`, `qvest/index.html`, `qvest/pyproject.toml`.
- `tekstream`: Currently empty. Ask for requirements and expected structure before creating files.
