# Tekstream Project Instructions

Purpose
- Implement the TekStream AI/ML Architect technical assessment in this folder.

Working rules
- Keep all work inside `tekstream/` unless the user requests cross-project changes.
- Use `tekstream/README.md` and `tekstream/TekStream AIML Architect Technical Assessment.pdf` as the source of truth for the assessment scope and deliverables.
- Prefer placing implementation artifacts under `tekstream/assessment/` following the specified structure.
- If requirements are ambiguous, summarize assumptions in `assessment/README.md`.
